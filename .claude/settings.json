{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"[hook] Edited Python file: $f\"; } 2>/dev/null || true",
            "if": "Edit(*.py)|Write(*.py)"
          }
        ]
      }
    ]
  }
}
